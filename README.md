# Notas:

Prueba de programa en Node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -n, --numero   Numero al cual se va a multiplicar         [número] [requerido]
  -l, --listar   Muestra la tabla en consola         [booleano] [defecto: false]
  -h, --hasta    Numer hasta el cual se va a multiplicar    [número] [requerido]